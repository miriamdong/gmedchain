/*
 * Products Messages
 *
 * This contains all the text for the Products component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Products';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Products component!',
  },
});
