/*
 * Pricing Messages
 *
 * This contains all the text for the Pricing component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Pricing';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Pricing component!',
  },
});
