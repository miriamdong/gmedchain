/*
 * SingleProduct Messages
 *
 * This contains all the text for the SingleProduct component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SingleProduct';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SingleProduct component!',
  },
});
